<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_member_expense_summary_dialog"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="24dp"
    android:paddingEnd="16dp"
    android:paddingBottom="24dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="32dp">

        <TextView
            android:id="@+id/txtBalanceDialogTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:ellipsize="end"
            android:fontFamily="@font/rajdhani_semibold"
            app:fontFamily="@font/rajdhani_semibold"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:paddingEnd="40dp"
            android:text="Detalle de deuda"
            android:textColor="?attr/expensePrimaryTextColor"
            android:textSize="24sp" />

        <ImageButton
            android:id="@+id/btnBalanceDialogClose"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="top|end"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/close"
            android:padding="0dp"
            android:src="@drawable/ic_close_simple"
            app:tint="?attr/trackerSummaryLabelColor" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/member1BalanceBlock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/txtBalanceMember1Name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/rajdhani_semibold"
            app:fontFamily="@font/rajdhani_semibold"
            android:includeFontPadding="false"
            android:textColor="?attr/trackerSummaryLabelColor"
            android:textSize="14sp"
            />

        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember1Spent"/>
        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember1Proportional" />
        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember1Balance" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:background="?attr/memberExpenseDialogDividerColor" />

    <LinearLayout
        android:id="@+id/member2BalanceBlock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/txtBalanceMember2Name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/rajdhani_semibold"
            app:fontFamily="@font/rajdhani_semibold"
            android:includeFontPadding="false"
            android:textColor="?attr/trackerSummaryLabelColor"
            android:textSize="14sp" />

        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember2Spent" />
        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember2Proportional" />
        <include layout="@layout/item_balance_detail_row" android:id="@+id/rowMember2Balance" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:background="?attr/memberExpenseDialogDividerColor" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/txtBalanceDebtLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="@font/rajdhani_semibold"
            app:fontFamily="@font/rajdhani_semibold"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="?attr/expensePrimaryTextColor"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/txtBalanceDebtAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:fontFamily="@font/rajdhani_semibold"
            app:fontFamily="@font/rajdhani_semibold"
            android:includeFontPadding="false"
            android:textColor="?attr/expensePrimaryTextColor"
            android:textSize="18sp" />
    </LinearLayout>
</LinearLayout>
